// UI refinements layered over the self-contained prototype.
modal=function(html,options={}){const d=document.getElementById('dialog');const title=options.title||'รายละเอียด';const sub=options.sub||'';d.setAttribute('role','dialog');d.setAttribute('aria-modal','true');d.setAttribute('aria-label',title);d.innerHTML=`<div class="modal-shell"><header class="modal-top"><div class="modal-title"><div class="eyebrow">${options.kicker||'siUUU DETAIL'}</div><h2>${title}</h2>${sub?`<p class="muted">${sub}</p>`:''}</div><button class="icon-btn" aria-label="ปิดหน้าต่าง" onclick="closeModal()">×</button></header><div class="modal-body">${html}</div><footer class="modal-foot"><button class="btn alt" onclick="closeModal()">ปิด</button>${options.primary||''}</footer></div>`;document.getElementById('modal').classList.add('open');document.body.style.overflow='hidden';setTimeout(()=>d.querySelector('.icon-btn')?.focus(),20)};
closeModal=function(){document.getElementById('modal').classList.remove('open');document.body.style.overflow=''};
document.addEventListener('keydown',e=>{if(e.key==='Escape'&&document.getElementById('modal').classList.contains('open'))closeModal()});
Object.assign(taskData[0],{level:'เริ่มต้น',summary:'สกัดประเด็นสำคัญจากความคิดเห็นผู้ใช้ 25 รายการ แล้วสรุปให้ทีมผลิตภัณฑ์ตัดสินใจต่อได้',deadline:'ส่งภายใน 90 นาที',description:'ทีมผลิตภัณฑ์ต้องการเข้าใจปัญหาหลักจาก Feedback จำลอง 25 รายการ เพื่อเลือกหัวข้อที่ควรแก้ในรอบถัดไป งานนี้เป็น Practice Mode และไม่มีข้อมูลลูกค้าจริง',skills:['Communication','Data Analysis','Problem Solving'],deliverables:['สรุป Insight ไม่เกิน 1 หน้า','จัดกลุ่มปัญหา 3–5 กลุ่ม','ข้อเสนอแนะที่ทำต่อได้ 2 ข้อ'],criteria:['แยกข้อเท็จจริงออกจากข้อสันนิษฐาน','สื่อสารกระชับและเห็นลำดับความสำคัญ','อ้างอิง Feedback ที่รองรับแต่ละ Insight'],complexity:'ต่ำ — ขอบเขตและ Template ชัดเจน',criticality:'ต่ำ — ใช้ข้อมูลจำลองเท่านั้น',support:'สูง — มีตัวอย่างและ AI Feedback ระหว่างทำ',skillGap:'ต่ำ — ตรงกับ Potential Skills ที่พบ',evidence:'ต่ำ — มี Simulation เดียว จึงใช้เพื่อสร้างหลักฐานเพิ่ม',workload:'เหมาะสม — จบได้ใน Session เดียว',fitReason:'โจทย์ตรงกับพฤติกรรมที่สังเกตจาก Challenge และมีโครงช่วยลดความคลุมเครือ',riskReason:'ข้อมูลจำลอง ไม่มีผลกระทบต่อลูกค้าหรือการตัดสินใจจริง',behaviors:['การจัดลำดับประเด็น','การระบุ Assumption','การสื่อสาร Insight']});
Object.assign(taskData[1],{level:'ปานกลาง',summary:'เปลี่ยนคำถามลูกค้าที่กระจัดกระจายให้เป็น FAQ ที่ทีมบริการใช้งานและแก้ไขต่อได้',deadline:'ส่งภายใน 2 วัน',description:'CloudCraft SME มีคำถามลูกค้าจำลอง 42 รายการจากหลายช่องทาง ต้องการ FAQ รุ่นทดลองเพื่อให้ทีมบริการตอบได้สม่ำเสมอ ก่อนเผยแพร่จะมี Reviewer ตรวจข้อความทุกข้อ',skills:['Communication','Information Synthesis','Customer Empathy'],deliverables:['FAQ จำนวน 8 ข้อพร้อมคำตอบ','รายการคำถามที่ยังต้องขอข้อมูลเพิ่ม','Revision log หลังรับ Feedback 1 รอบ'],criteria:['คำตอบถูกต้องตามข้อมูลต้นทาง','ภาษาชัดเจนและเหมาะกับลูกค้า','ระบุขอบเขตเมื่อข้อมูลยังไม่พอ'],complexity:'ปานกลาง — ต้องรวมคำถามซ้ำและจัดลำดับ',criticality:'ต่ำ — Reviewer ตรวจครบก่อนเผยแพร่',support:'สูง — มี Brand guide และ Reviewer',skillGap:'ปานกลาง — ยังไม่มี Evidence ด้าน Customer Empathy',evidence:'ต่ำ — Communication ถูกสังเกตจาก Simulation เดียว',workload:'เหมาะสม — แบ่งทำ 2 รอบภายใน 2 วัน',fitReason:'มีสัญญาณการสื่อสารข้อจำกัดชัดเจน แต่ควรมี Reviewer ช่วยตรวจ Tone of voice',riskReason:'งานย้อนแก้ได้และยังไม่เผยแพร่จนกว่า Reviewer อนุมัติ',behaviors:['การถาม Requirement','การสื่อสารข้อมูลไม่ครบ','การรับ Feedback และแก้งาน']});
Object.assign(taskData[2],{level:'ท้าทาย',summary:'วิเคราะห์สัญญาณการเลิกใช้บริการจาก Dashboard และเขียน Action memo สำหรับทีมดูแลลูกค้า',deadline:'ส่งภายใน 5 วัน',description:'Nova Retail ต้องการเข้าใจปัจจัยที่สัมพันธ์กับ Churn จากข้อมูลจำลอง 6 เดือน ผลลัพธ์ใช้ในการ Workshop ภายใน แต่ยังไม่ใช้ตัดสินใจต่อลูกค้ารายบุคคล',skills:['Data Analysis','Problem Solving','Business Communication'],deliverables:['Notebook หรือไฟล์ Query ที่ตรวจซ้ำได้','Dashboard สรุป 3 มุมมอง','Action memo ไม่เกิน 2 หน้า'],criteria:['วิธีวิเคราะห์ตรวจย้อนกลับได้','ไม่สรุปเหตุและผลเกินข้อมูล','ข้อเสนอเชื่อมกับ Insight อย่างชัดเจน'],complexity:'สูง — มีหลาย Deliverables และตัวแปรสัมพันธ์กัน',criticality:'ปานกลาง — ใช้ประกอบ Workshop ภายใน',support:'ปานกลาง — มี Mentor check-in 2 ครั้ง',skillGap:'สูง — ยังไม่มี Work Evidence ด้าน Churn',evidence:'ต่ำ — หลักฐานปัจจุบันยังเป็น Simulation',workload:'ต้องทบทวน — ใช้เวลาประมาณ 8–10 ชั่วโมง',fitReason:'เป็นโอกาสพัฒนาระยะถัดไป แต่ Evidence ปัจจุบันยังไม่พอสำหรับทำโดยไม่มี Mentor',riskReason:'วิเคราะห์ผิดอาจทำให้ทีมวางสมมติฐานผิด จึงต้องมี Mentor และ Review ก่อนนำเสนอ',behaviors:['การตรวจคุณภาพข้อมูล','การอธิบายความไม่แน่นอน','การตอบ Feedback เชิงเทคนิค']});
tasks=function(){let list=S.taskFilter==='ทั้งหมด'?taskData:taskData.filter(x=>x.type===S.taskFilter);V.innerHTML=`<div class="split-head"><div><div class="eyebrow">STEP 5 · Grow</div><h1>Evidence Opportunities</h1><p class="lead">งานแต่ละชิ้นบอกขอบเขต สิ่งที่ต้องส่ง เกณฑ์ประเมิน และเงื่อนไขควบคุมความเสี่ยงก่อนตัดสินใจรับ</p></div>${S.acceptedTask?`<span class="badge">มีภารกิจในแผน 1 งาน ✓</span>`:''}</div><div class="filters">${['ทั้งหมด','Practice','Employer Micro-task','Real Employer Mission'].map(f=>`<button class="${S.taskFilter===f?'active':''}" onclick="S.taskFilter='${f}';save();tasks()">${f}</button>`).join('')}</div><div class="grid">${list.map(t=>`<article class="card task job-card"><div class="job-top"><span class="badge">${t.type}</span><span class="job-level">${t.level}</span></div><h3 style="margin-top:13px">${t.title}</h3><p class="muted">${t.company} · ${t.deadline}</p><p class="muted job-summary">${t.summary}</p><div class="tag-row">${t.skills.map(x=>`<span class="skill-tag">${x}</span>`).join('')}</div><div class="fit-boxes"><div class="fit-box"><small>Completion Fit</small><b>${t.fit}</b></div><div class="fit-box"><small>Business Risk</small><b>${t.risk}</b></div></div><div class="decision-line">คำแนะนำ: ${t.decision}</div><div class="actions job-actions"><button class="btn small" onclick="acceptTask('${t.id}')">ดูรายละเอียดงาน</button>${S.acceptedTask===t.id?'<span class="badge">อยู่ในแผนแล้ว ✓</span>':''}</div></article>`).join('')}</div><div class="notice">Completion Fit และ Business Risk เป็นผลลัพธ์คนละด้าน ใช้ช่วยกำหนดเงื่อนไขงาน ไม่ใช่คำรับประกันความสำเร็จ</div>`};
acceptTask=function(id){const t=taskData.find(x=>x.id===id);const body=`<section class="detail-hero"><div class="eyebrow" style="color:#8de0d5">${t.type} · ${t.level}</div><h3>${t.summary}</h3><p>${t.description}</p></section><div class="detail-grid"><div><section class="detail-section"><h4>สิ่งที่ต้องส่ง (Deliverables)</h4><ul class="detail-list">${t.deliverables.map(x=>`<li>${x}</li>`).join('')}</ul></section><section class="detail-section"><h4>เกณฑ์ประเมิน</h4><ul class="detail-list">${t.criteria.map(x=>`<li>${x}</li>`).join('')}</ul></section><section class="detail-section"><h4>Behavioral Evidence ที่ออกแบบให้สังเกต</h4><div class="tag-row">${t.behaviors.map(x=>`<span class="behavior-tag">${x}</span>`).join('')}</div></section></div><aside><section class="detail-section"><h4>AI วิเคราะห์งาน</h4><div class="analysis-row"><span>Task Complexity</span><b>${t.complexity}</b></div><div class="analysis-row"><span>Task Criticality</span><b>${t.criticality}</b></div><div class="analysis-row"><span>Support Level</span><b>${t.support}</b></div></section><section class="detail-section"><h4>AI วิเคราะห์ผู้ใช้ × งาน</h4><div class="analysis-row"><span>Skill Gap</span><b>${t.skillGap}</b></div><div class="analysis-row"><span>Evidence Confidence</span><b>${t.evidence}</b></div><div class="analysis-row"><span>Time & Workload Fit</span><b>${t.workload}</b></div></section><section class="detail-section"><h4>Matching Decision</h4><div class="decision-line">${t.decision}</div><p class="muted"><b>Completion Fit ${t.fit}:</b> ${t.fitReason}</p><p class="muted"><b>Business Risk ${t.risk}:</b> ${t.riskReason}</p></section></aside></div><div class="deadline-box"><div><small>กำหนดส่ง</small><br><b>${t.deadline}</b></div><span class="badge">Support: ${t.support.split(' — ')[0]}</span></div><p class="modal-note">ผลการจับคู่นี้ช่วยประเมินความเหมาะสมและควบคุมความเสี่ยงตาม Evidence ที่มี ไม่รับประกันว่าผู้ใช้จะทำสำเร็จหรือบริษัทจะไม่มีความเสี่ยง</p>`;const primary=S.acceptedTask===id?`<button class="btn teal" onclick="closeModal();toast('งานนี้อยู่ในแผนของคุณแล้ว')">อยู่ในแผนแล้ว ✓</button>`:`<button class="btn" onclick="S.acceptedTask='${id}';save();closeModal();tasks();toast('เพิ่มภารกิจในแผนสร้าง Evidence แล้ว')">รับภารกิจนี้</button>`;modal(body,{title:t.title,sub:t.company,kicker:'JOB DETAIL',primary})};
evidenceModal=function(){const body=`<div class="detail-grid"><div><section class="detail-section"><h4>Candidate Response</h4><div class="evidence-snippet">${esc(S.answer||'ยังไม่มีคำตอบใน Session นี้')}</div></section><section class="detail-section"><h4>Revision หลังเงื่อนไขเปลี่ยน</h4><div class="evidence-snippet">${esc(S.revision||'ยังไม่มี Revision ใน Session นี้')}</div></section></div><aside><section class="detail-section"><h4>Audit Timeline</h4><div class="timeline"><p><b>Situation opened</b><br><span class="muted">ข้อมูลขาด 23% · Deadline 2 ชั่วโมง</span></p><p><b>Adaptive event</b><br><span class="muted">Deadline เหลือ 45 นาที</span></p><p><b>Rubric assessment</b><br><span class="muted">Communication 3/5 · Problem Solving 4/5</span></p></div></section><section class="detail-section"><h4>Integrity Status</h4><div class="notice green" style="margin:0">ไม่พบ Anomaly ที่ต้องพักผล</div></section></aside></div>`;modal(body,{title:'Evidence Trace',sub:'Audit reference #SIM-2481 · Revision history ตรวจย้อนหลังได้',kicker:'AUDIT & EVIDENCE'})};
P[0][1]='เริ่มต้น';const firstNav=document.querySelector('.nav button[data-id="overview"]');if(firstNav){firstNav.querySelector('span').textContent='เริ่มต้น'}
overview=function(){V.innerHTML=`<section class="entry-page" aria-label="หน้าเริ่มต้น siUUU"><div class="entry-inner"><div class="brand-symbol" aria-hidden="true"><i class="signal one"></i><i class="signal two"></i><i class="base"></i></div><h1 class="entry-wordmark">si<span>UUU</span></h1><p class="entry-tagline">Turn Potential into Proof</p><p class="entry-copy">พื้นที่ที่ช่วยเปลี่ยนประสบการณ์ โปรเจกต์ และสิ่งที่คุณลงมือทำ ให้กลายเป็นหลักฐานความสามารถที่มองเห็นได้</p><div class="entry-action"><button class="btn" onclick="go('story')">เริ่มเล่าเรื่องของคุณ →</button></div><div class="entry-dots" aria-hidden="true"><i></i><i></i><i></i></div><p class="entry-note">Evidence &gt; Credentials · Hackathon Prototype</p></div></section>`};
render=function(){document.body.classList.toggle('brand-entry',S.page==='overview');updateChrome();({overview,story,challenge,assessment,rank,tasks,profile,match,employer,governance}[S.page]||overview)()};render();
